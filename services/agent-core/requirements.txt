# Pinned to the versions verified locally end-to-end (see docs/SPIKE.md gate c).
#
# Do NOT pin fastapi below google-adk's floor: adk 2.7.1 requires fastapi>=0.133,
# and pinning 0.115.6 made Cloud Build fail with ResolutionImpossible while the
# local venv passed -- it already had a compatible fastapi resolved.
google-adk==2.7.1
fastapi==0.141.1
uvicorn[standard]==0.52.4

# SWARM WO1-4: Firestore-backed case store (contract §3.1) and the Pub/Sub
# push subscriber (§3.2). CaseStore/pubsub_client fall back to in-memory /
# logged-no-op when these can't reach a real project (see agent_core/store.py,
# agent_core/pubsub_client.py) so local dev and CI never need real credentials.
google-cloud-firestore==2.29.0
google-cloud-pubsub==2.39.2

# BUG (persona 5 WO6 task 1, found live 2026-08-25): `infra/deploy.sh` stages
# packages/delivery's SOURCE into this service's build context (see
# `pkgs_for`), but this service's own Dockerfile only ever
# `pip install -r requirements.txt` from THIS file -- RELAY's
# `packages/delivery/requirements.txt` is never installed here. Locally this
# never surfaced (pypdf/reportlab were already in the dev venv from working
# in the monorepo), so every test and manual run rendered real PDFs fine --
# but the deployed container had `delivery.pdf.engine`/`delivery.vendors.*`
# importable while their actual runtime dependencies were entirely missing,
# and both are imported lazily (see packages/delivery/pdf/engine.py's own
# docstring for why) so nothing failed until the FIRST real filing request:
# `ModuleNotFoundError: No module named 'pypdf'` inside `_fill_acroform`,
# 500ing every `approve_filing` call. Versions match
# packages/delivery/requirements.txt exactly -- one source of truth for RELAY's
# own pins, duplicated here only because Docker has no cross-service install.
pypdf==6.16.2
reportlab==5.0.1
requests==2.34.2
google-cloud-storage==3.13.1
